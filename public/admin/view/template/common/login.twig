{{ header }}
<header id="content">
  <!-- Background image -->
  <div id="intro" class="bg-image shadow-2-strong">
    <div class="mask d-flex align-items-center h-100" style="background-color: rgba(0, 0, 0, 0.9);">
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-xl-5 col-md-8">
            <form class="bg-white-transparent rounded shadow-5-strong p-5" action="{{ action }}" method="post" enctype="multipart/form-data">
              <h2>{{ text_login }}</h2>

              {% if error_warning %}
                <p class="note note-danger note-sm">{{ error_warning }}</p>
              {% endif %}

              <!-- Email input -->
              <div class="form-outline mb-4">
                <label class="form-label" for="input-login">{{ entry_username }}</label>
                <input name="username" type="text" id="input-login" class="form-control" />
              </div>

              <!-- Password input -->
              <div class="form-outline mb-4">
                <label class="form-label" for="input-password">{{ entry_password }}</label>
                <input name="password" type="password" id="input-password" class="form-control" />
              </div>

              <!-- 2 column grid layout for inline styling -->
              <div class="row mb-4">
                <div class="col d-flex justify-content-center"></div>

                <div class="col text-center">
                  <!-- Simple link -->
                  {% if forgotten %}
                    <a href="{{ forgotten }}">{{ text_forgotten }}</a>
                  {% endif %}
                </div>
              </div>

              <!-- Submit button -->
              <button type="submit" class="btn btn-primary btn-block">{{ button_login }}</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
  <!-- Background image -->
</header>
{{ footer }}